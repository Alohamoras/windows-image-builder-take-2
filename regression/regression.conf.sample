# regression.conf — Configuration for the wimsy regression test suite.
#
# Copy this file to regression/regression.conf and adjust for your environment.
#   cp regression/regression.conf.sample regression/regression.conf

# ---------------------------------------------------------------------------
# Paths
# ---------------------------------------------------------------------------

# Directory where ISOs are stored / downloaded to
ISO_DIR=/path/to/iso-downloads

# Directory where built .raw images land
OUTPUT_DIR=/path/to/build-output

# ---------------------------------------------------------------------------
# Shared build inputs (same as imgbuild.env)
# ---------------------------------------------------------------------------

VIRTIO_ISO=/path/to/iso-downloads/virtio-win.iso

# Default unattend directory; may be overridden per-version below
UNATTEND_DIR=./unattend

OVMF_PATH=/usr/share/OVMF/OVMF_CODE_4M.fd

WORK_DIR=/tmp

# ---------------------------------------------------------------------------
# Versions to test (space-separated; subset allowed)
# ---------------------------------------------------------------------------
VERSIONS="2016 2019 2022 2025"

# ---------------------------------------------------------------------------
# Per-version unattend overrides (leave empty to use UNATTEND_DIR)
# ---------------------------------------------------------------------------
UNATTEND_DIR_2016=
UNATTEND_DIR_2019=
UNATTEND_DIR_2022=
UNATTEND_DIR_2025=

# ---------------------------------------------------------------------------
# ISO download URLs
# Adjust if a Microsoft redirect resolves to the wrong version.
# ---------------------------------------------------------------------------
ISO_URL_2016="https://go.microsoft.com/fwlink/p/?LinkID=2195280"
ISO_URL_2019="https://go.microsoft.com/fwlink/p/?LinkID=2195167"
ISO_URL_2022="https://go.microsoft.com/fwlink/p/?LinkID=2195174"
ISO_URL_2025="https://go.microsoft.com/fwlink/?linkid=2345730"
